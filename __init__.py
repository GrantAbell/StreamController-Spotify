"""StreamController plugin providing Spotify control for keys and Stream Deck+ dials.

StreamController imports this directory as `plugins.<plugin_id>`, so every
internal import is relative. Keep this file free of GTK and StreamController
imports so `spotify/` and `rendering/` stay importable in a bare test process.
"""
